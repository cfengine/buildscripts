#!/bin/sh

. `dirname "$0"`/functions
. version

set -x

BUILDMACHINE="$1"

mkdir -p $BASEDIR/../../../output/.tmp/${SCHEDULER}/${VERSION}
rsync -ar "$BUILDMACHINE:build/output/*" $BASEDIR/../../../output/.tmp/${SCHEDULER}/${VERSION}

ssh "$BUILDMACHINE" "rm -rf build/output"
