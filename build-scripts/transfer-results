#!/bin/sh

. `dirname "$0"`/functions
. version

set -x

BUILDMACHINE="$1"

mkdir -p $BASEDIR/../../../output/.tmp/${SCHEDULER}/${BUILD_NUMBER}
rsync -ar "$BUILDMACHINE:build/output/*" $BASEDIR/../../../output/.tmp/${SCHEDULER}/${BUILD_NUMBER}

ssh "$BUILDMACHINE" "rm -rf build/output"
