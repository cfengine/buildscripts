#!/bin/sh

if [ -f /usr/local/sbin/cf-agent ]; then
	rm -f /usr/local/sbin/cf-agent
	rm -f /usr/local/sbin/cf-execd
	rm -f /usr/local/sbin/cf-key
	rm -f /usr/local/sbin/cf-monitord
	rm -f /usr/local/sbin/cf-promises
	rm -f /usr/local/sbin/cf-runagent
	rm -f /usr/local/sbin/cf-serverd
fi

exit 0
